local inoremap = vim.keymap.inoremap
local ts_utils = require('nvim-treesitter.utils')

local bracket_pair = function()
  local node = ts_utils.get_current_node()
  local previous_node = f
end





-- inoremap { '(', function() end }
